# Required. Use a stable /dev/serial/by-id path when possible. Set this to
# "auto" only when automatic discovery of the configured USB ID is desired.
RELAY_DEVICE=
RELAY_USB_VENDOR=
RELAY_USB_PRODUCT=
RELAY_WAIT_SECONDS=10
